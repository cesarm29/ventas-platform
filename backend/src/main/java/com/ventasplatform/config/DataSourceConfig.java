package com.ventasplatform.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class DataSourceConfig {
    // Removed - using Spring Boot auto-config with SPRING_DATASOURCE_URL env var
}
